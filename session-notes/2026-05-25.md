# Session Notes — 2026-05-25

## Completed

- **CSP header added**: Added `Content-Security-Policy` to `netlify.toml` `/*` headers block. Reviewed a proposed CSP from another session, identified three issues (asset-tidycal.b-cdn.net in wrong directive, missing GA4 region endpoints, missing preconnect coverage in connect-src), and applied the corrected version.

## Pending

- Compliance screenshots after pushing to `jay/main` per the project compliance memory.
- After deploy, check Chrome DevTools Console for CSP violations and adjust if needed.
