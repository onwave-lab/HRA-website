# Session Notes — 2026-05-20

## Completed

- **Calendar facade clickable**: made the whole `.calendar-facade` card open the TidyCal booking modal on `index.html`, `about.html`, and `services.html`. Added `data-booking` to the wrapper div (existing site-wide click delegation in `js/booking-modal.js:162` catches it) and `cursor: pointer` to the `.calendar-facade` CSS rule. Kept `aria-hidden="true"` on the wrapper so screen readers still only see the inner CTA link — avoids duplicate "Schedule a consultation" controls. No change to hover effects on the inner CTA button.

## Pending

- Compliance screenshots after pushing to `jay/main` per the project compliance memory.
